---
"@siluat/color-contrast-cli": minor
---

Add `--batch` flag for processing multiple color pairs from stdin. Supports `--json`, `--level`, and `--suggest` combinations. Input uses bracket-aware splitting to handle functional colors like `rgb()` and `oklch()`.

Related PRs: #104, #105, #106, #107, #108
