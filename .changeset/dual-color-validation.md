---
"@siluat/color-contrast-cli": minor
---

Add `validateColors` function that checks both foreground and background colors upfront and reports all errors at once, instead of failing on the first invalid color
