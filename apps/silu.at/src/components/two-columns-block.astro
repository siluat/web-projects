---
import { grid, gridItem } from '@/styled-system/patterns'
---

<div class={grid({ columns: 2, h: '100%', gap: 0 })}>
  <div class={gridItem({ h: '100%' })}><slot name="left-column" /></div>
  <div class={gridItem({ h: '100%' })}><slot name="right-column" /></div>
</div>
